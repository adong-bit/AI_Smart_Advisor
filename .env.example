# 复制本文件为 .env 后填写。与 app.py 放在同一目录。

# 若站点挂在子路径（如 https://域名/app/），请填写无前导斜杠或带斜杠均可，保存后重启 Flask
# FLASK_APPLICATION_ROOT=/app

# Kimi（Moonshot）API：https://platform.moonshot.cn 申请密钥（二选一即可）
KIMI_API_KEY=
# MOONSHOT_API_KEY=

# 说明：KIMI_API_KEY / MOONSHOT_API_KEY / KIMI_MODEL / KIMI_API_URL 以本文件为准，
# 会覆盖终端里 export 的旧值（避免「改了 .env 仍不生效」）。

# 可选：模型名以控制台「模型列表」为准；旧版 moonshot-v1-8k 若不可用请改为当前文档中的 ID
# KIMI_MODEL=moonshot-v1-8k
# 设为 0 可关闭快讯 Kimi（快讯将显示为中性）
# NEWS_USE_KIMI=1
# 设为 0 可关闭「AI 市场洞察」的 Kimi 生成（将显示不可用提示）
# INSIGHTS_USE_KIMI=1
